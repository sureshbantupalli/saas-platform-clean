from django.apps import AppConfig


class RolesConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.settings.roles'
    label = 'settings_roles'
    verbose_name = 'Settings — Roles & Permissions'
