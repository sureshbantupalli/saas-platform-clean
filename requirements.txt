# ANJASI platform — Python dependencies
# NOTE: this file must stay UTF-8. It was previously saved as UTF-16-LE,
# which `pip install -r requirements.txt` cannot parse on Linux.

# ── Core framework ─────────────────────────────────────────
asgiref==3.11.1
Django==6.0.4
djangorestframework==3.17.1
sqlparse==0.5.5

# ── Database ───────────────────────────────────────────────
psycopg2-binary==2.9.11

# ── Utilities ──────────────────────────────────────────────
python-dateutil==2.9.0.post0
python-dotenv==1.2.2
six==1.17.0
tzdata==2026.1
cryptography

# ── HTTP client ────────────────────────────────────────────
# Used directly by the MSG91 SMS adapter (and, later, the WhatsApp
# and SES adapters). Previously only present as a transitive dep.
requests==2.33.1

# ── Payments ───────────────────────────────────────────────
razorpay==2.0.1

# ── Production serving ─────────────────────────────────────
# Required to run under a real web server; `manage.py runserver`
# must never be used in production.
gunicorn==23.0.0
whitenoise==6.8.2

# ── Object storage (media) ─────────────────────────────────
# Needed only when MEDIA_STORAGE_BACKEND=s3|spaces, but installed always so
# settings can reference the backend without a conditional import. Covers both
# AWS S3 and DigitalOcean Spaces — Spaces is S3-compatible; only the endpoint
# URL differs.
django-storages==1.14.4
boto3==1.35.36

# ── Media ──────────────────────────────────────────────────
# Django's ImageField REQUIRES Pillow. Three ImageFields exist
# (apps/assessments/models.py, apps/settings/branding/models.py);
# migrations fail on a clean environment without it.
Pillow==11.1.0
